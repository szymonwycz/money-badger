{
  "_comment": "Copy to config.json and edit. IBANs are stored without the country prefix and without spaces. Relative paths resolve against the project root.",

  "output_dir": "Export",
  "corrections_dir": "Corrections",

  "_comment_accounts": "Map each IBAN to the account name you created in the app. The names must match exactly or imported transactions land on the wrong account.",
  "account_names": {
    "00000000000000000000000001": "Main account",
    "00000000000000000000000002": "Partner account"
  },
  "own_ibans": [
    "00000000000000000000000001",
    "00000000000000000000000002"
  ],

  "_comment_savings": "Money arriving from these accounts is income from your own savings, not new income. Leave empty if you have none.",
  "savings_ibans": [],

  "_comment_filters": "Banks word the same event differently, so the phrases that classify a row live here rather than in the code. All matching is lowercase substring matching against the transaction title plus counterpart name.",
  "filters": {
    "own_transfer_titles": ["between your own accounts"],

    "_comment_transfer_to_account": "A rule fires when EVERY phrase in match appears. Use it for rows that are really a transfer into another account you track — a cash withdrawal is a transfer into your cash account.",
    "transfer_to_account": [
      {"match": ["atm withdrawal"], "account": "Cash"}
    ],

    "savings_withdrawal_titles": [],

    "_comment_skip_incoming": "Card top-ups book both legs without an IBAN, days apart. Keep only the outgoing leg or the money is counted twice.",
    "skip_incoming_titles": ["top-up"]
  },

  "_comment_split": "One transfer that actually pays several things. Exactly one part takes \"remainder\" and absorbs whatever the fixed parts leave. total_range is the sanity check that keeps unrelated payments to the same account intact. Leave empty unless you have such a payment.",
  "split_rules": [],

  "_comment_hints": "Extra instructions appended to the categorization prompt. Use them for local knowledge the category names alone don't convey.",
  "prompt_hints": [
    "EV charging stations belong under car fuel, not utilities"
  ],

  "batch_size": 30,

  "_comment_order_matching": "Order confirmation emails matched to the card payments they explain, so a row reading only AMAZON gets the product names and a category. Each shop is read from its own mail folder — on Gmail, a label. Mailbox credentials live in .env (MAIL_ADDRESS / MAIL_PASSWORD). Delete this block to read Allegro and nothing else.",
  "order_matching": {
    "_comment_shops": "Allegro is parsed from its own template. Every other shop is read by the LLM, which costs a fraction of a cent per email and needs no knowledge of the template. The key is the name written into the transaction description.",
    "shops": {
      "Allegro": {"folder": "Allegro"},
      "Amazon": {"folder": "Amazon"}
    },

    "_comment_fx": "A foreign order reaches the statement converted at the bank's own rate, so it is matched within fx_tolerance_pct of the rate you give here. A currency with no rate is never matched, only reported for manual review. local_currency is what your accounts are denominated in.",
    "local_currency": "PLN",
    "fx_rates": {"EUR": 4.30, "USD": 4.00},
    "fx_tolerance_pct": 5
  },

  "_comment_eb": "Enable Banking credentials. Register your own application at enablebanking.com, download the private key, and put the app id here. See docs/ENABLE_BANKING.md.",
  "enable_banking": {
    "app_id": "",
    "private_key_path": "sync/enable_banking.pem",
    "state_file": "sync/.bank_sync_state.json",
    "days_back": 30,

    "_comment_aspsp": "Which bank each IBAN belongs to, as Enable Banking names them. default_aspsp covers every IBAN not listed here.",
    "default_aspsp": {"name": "", "country": ""},
    "aspsp_names": {}
  }
}
